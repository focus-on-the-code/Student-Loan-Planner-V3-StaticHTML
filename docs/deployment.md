# Deployment

This app deploys directly from static repository files through GitHub Pages. Keep `.nojekyll`, relative paths, and no-build browser modules intact. After merging to the configured Pages branch, verify `/`, `/index.html`, CSS, JS modules, and brand assets return HTTP 200.
