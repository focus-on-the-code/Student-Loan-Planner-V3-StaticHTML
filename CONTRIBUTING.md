# Contributing

Keep contributions static, privacy-first, and source-backed. Do not add frameworks, package dependencies, build steps, analytics, or server requirements. Include tests and update `docs/test-reports/` for meaningful behavior changes.
