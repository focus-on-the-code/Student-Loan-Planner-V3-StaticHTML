// @ts-check
export function renderSvgLineChart(plans, { width = 720, height = 240 } = {}) { const max = Math.max(1, ...plans.flatMap((p) => (p.yearly || []).slice(0,10).map((y) => y.paymentCents || 0))); const pad = 34; const colors = ['#2e8cff','#ff76c7','#ffb15c','#8f73ff','#1a8e66']; const lines = plans.map((plan, index) => { const points = (plan.yearly || []).slice(0,10).map((year, i) => `${pad + i * ((width - pad * 2) / 9)},${height - pad - ((year.paymentCents || 0) / max) * (height - pad * 2)}`).join(' '); return `<polyline tabindex="0" points="${points}" fill="none" stroke="${colors[index % colors.length]}" stroke-width="4" aria-label="${plan.name}"/>`; }).join(''); return `<svg class="payment-chart" viewBox="0 0 ${width} ${height}" role="img" aria-label="Annual payment path chart"><line x1="${pad}" y1="${height-pad}" x2="${width-pad}" y2="${height-pad}" stroke="#9aa6c0"/><line x1="${pad}" y1="${pad}" x2="${pad}" y2="${height-pad}" stroke="#9aa6c0"/>${lines}</svg>`; }
