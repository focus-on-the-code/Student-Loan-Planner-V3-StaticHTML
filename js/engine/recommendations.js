// @ts-check
export function buildRecommendations({ rankedPlans, allPlans }) { const lowest = rankedPlans[0]; const items = []; if (lowest) items.push({ type: 'lowest-current-payment', planId: lowest.planId, text: `${lowest.name} has the lowest estimated current monthly payment among clearly eligible plans.` }); const forgiving = allPlans.filter((p) => p.estimatedForgivenessCents > 0).sort((a,b)=>b.estimatedForgivenessCents-a.estimatedForgivenessCents)[0]; if (forgiving) items.push({ type: 'possible-forgiveness', planId: forgiving.planId, text: `${forgiving.name} shows possible remaining balance at forgiveness, subject to eligibility and tax assumptions.` }); return items; }
