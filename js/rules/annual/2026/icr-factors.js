// @ts-check

export const icrFactors2026 = Object.freeze({
  year: 2026,
  status: 'scaffold',
  sourceIds: Object.freeze(['fsa-icr-plan']),
  factors: Object.freeze([]),
  notes: Object.freeze([
    'Milestone 2 creates the annual-data module boundary only.',
    'Production ICR factor values and interpolation tests will be added with formula implementation.'
  ])
});
