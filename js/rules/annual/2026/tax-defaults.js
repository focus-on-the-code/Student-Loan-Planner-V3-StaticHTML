// @ts-check

export const taxDefaults2026 = Object.freeze({
  year: 2026,
  status: 'planning-assumption-defaults',
  sourceIds: Object.freeze([]),
  federalForgivenessTaxRateBasisPoints: 2200,
  stateForgivenessTaxRateBasisPoints: 0,
  notes: Object.freeze([
    'Defaults are user-editable planning assumptions, not tax advice.',
    'Taxability and full tax liability are out of scope for Milestone 2.'
  ])
});
