# Agent Instructions

- Keep this a no-framework, no-build static GitHub Pages app.
- Do not add production package dependencies or require a package manager.
- Use relative URLs only; do not introduce root-absolute asset paths.
- Do not change branch-based Pages deployment assumptions without owner approval.
- Do not add or alter repayment formulas unless the formula has a source/rule ID and test coverage.
- Do not claim a check passed unless actual terminal output was produced.
- Report incomplete PRD requirements as incomplete; do not hide them behind “pass with warnings.”
